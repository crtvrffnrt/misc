# Misc
## Linux Helpers
### OneLiner ZSH Install
--> for current User
```bash
curl -fsSL https://patrick-binder.de/zsh | sudo bash
```
--> For all eligible users:
```bash
curl -fsSL https://patrick-binder.de/zsh | sudo bash -s -- --install-for-all-user
```
